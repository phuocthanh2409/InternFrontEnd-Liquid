<div class="page-width">
  <div class="empty-page-content">
    <h1>{{ 'general.404.title' | t }}</h1>
      <img src="{{ '404.png' | asset_url}}">
    <p>{{ 'general.404.subtext' | t }}</p>
    <a href="{{ routes.root_url }}" class="btn btn--has-icon-after">{{ 'general.404.link' | t }}</a>
  </div>
</div>
<style>
    .page-width{
      max-width: 1440px;
      margin: 0 auto;
    }
    .empty-page-content{
      padding-bottom: 134px;
      padding-top: 80px;
      text-align: center;
    }

    .empty-page-content img{
      max-width:309px;
      max-height: 148px;
      padding-bottom: 12px;
    }

    .empty-page-content h1{
      font-style: normal;
      font-weight: normal;
      font-size: 36px;
      line-height: 43px;
      text-align: center;
      color: #272E35;
      padding-bottom: 8px;
    }
    
    .empty-page-content p{
      font-style: normal;
      font-weight: normal;
      font-size: 18px;
      line-height: 21px;
      text-align: center;
      color: #7C7C7C;
      padding-bottom: 40px;
    }

    .empty-page-content a{
          border-radius: 2px;
          width: 320px;
          height: 48px;
          border: none;
          margin: 0 auto;
          display: block;
          text-decoration: none;
          background-color: #E94141;
          color: #ffffff;
          font-style: normal;
          font-weight: normal;
          font-size: 14px;
          line-height: 44px;
          text-align: center;
    }
</style>